{
  "name": "capture-page",
  "private": true,
  "type": "module",
  "description": "The page a phone opens to capture a photo and sign its receipt on the device",
  "scripts": {
    "wasm": "cargo build --profile wasm -p fingerprint --target wasm32-unknown-unknown --manifest-path ../Cargo.toml && mkdir -p public && cp ../target/wasm32-unknown-unknown/wasm/fingerprint.wasm public/fingerprint.wasm",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "@noble/hashes": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
